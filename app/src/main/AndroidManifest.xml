<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lucaspw8.gproductfirebase">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CadastroProduto"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".CadastroUsuario" />
        <activity android:name=".CadastroEmpresaActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MenuLateral"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MeuPerfilActivity" />
        <activity android:name=".EditarPerfil" />
        <activity android:name=".EditarEmpresa" />
        <activity android:name=".InformacaoProduto" />
        <activity android:name=".InformacaoProdutoEmpresa" />
        <activity android:name=".EditarProduto"></activity>
    </application>

</manifest>